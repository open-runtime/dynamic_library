name: dynamic_library
description: A utility package for improve dynamic library loading
version: 0.1.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.0 <3.0.0'

dependencies:
  path: ^1.8.0

dev_dependencies:
  runtime_lints: ^0.4.0
  test: ^1.16.0
